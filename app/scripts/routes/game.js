/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var GameRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return GameRouter;
});
